import { ComponentSingleStyleConfig } from '@chakra-ui/react'

export const Drawer: ComponentSingleStyleConfig = {
  baseStyle: {
    dialog: {
      bg: 'cardBg',
      // backdropFilter: 'blur(40px)',
    },
  },
}
